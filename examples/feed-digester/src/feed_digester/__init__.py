"""Feed Digester — RSS/newsletter to daily briefing.

Toolchain skeleton only. The pipeline stages (ingest, dedupe, cluster, render, deliver,
schedule) and the `Item` data contract (documented in CLAUDE.md, "Item schema") are the
backlog — the factory materializes them. This package exists so the build/test toolchain is
runnable from the first cycle.
"""

__version__ = "0.1.0"
